{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "TOM-web",
  "projectType": "application",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "continuous": true,
      "options": {
        "command": "nx run @org/web:dev"
      }
    }
  }
}
