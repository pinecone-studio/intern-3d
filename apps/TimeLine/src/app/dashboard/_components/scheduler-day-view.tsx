'use client'

import Link from 'next/link'
import { DAY_VIEW_SLOT_COUNT, DAY_VIEW_SLOT_INDEXES, DAY_VIEW_SLOT_MINUTES } from '@/app/dashboard/_lib/scheduler-constants'
import { eventOccursInWeekOnDay, getEventLabel, getEventTone } from '@/app/dashboard/_lib/scheduler-event-utils'
import { clampEventToPlanningDay, slotToTime, timeToMinutes } from '@/app/dashboard/_lib/scheduler-time-utils'
import type { Room, ScheduleEvent } from '@/lib/types'
import { cn } from '@/lib/utils'

export function SchedulerDayView({ errorMessage, events, focusedDateIso, focusedDateLabel, focusedDateTitle, focusedDayOfWeek, loading, onContextMenu, onEditEvent, onPointerDown, onPointerEnter, rooms, selection }: { errorMessage?: string; events: ScheduleEvent[]; focusedDateIso: string; focusedDateLabel: string; focusedDateTitle: string; focusedDayOfWeek: number; loading: boolean; onContextMenu: (_event: React.MouseEvent, _roomId: string, _dayOfWeek: number, _slot: number, _slotMinutes: number) => void; onEditEvent: (_event: ScheduleEvent, _roomId: string, _dayOfWeek: number) => void; onPointerDown: (_roomId: string, _dayOfWeek: number, _slot: number, _slotMinutes: number, _button: number) => void; onPointerEnter: (_roomId: string, _dayOfWeek: number, _slot: number) => void; rooms: Room[]; selection: { roomId: string; dayOfWeek: number; startSlot: number; endSlot: number } | null }) {
  return (
    <div className="min-w-[980px]">
      <div className="sticky top-0 z-[2] grid grid-cols-[104px_minmax(860px,1fr)] border-b border-[#e1dfdd] bg-white dark:border-border dark:bg-card"><div className="border-r border-[#e1dfdd] px-2.5 py-2 text-[11px] font-semibold uppercase text-muted-foreground dark:border-border">Анги</div><div className="px-3 py-2"><div className="flex items-center justify-between"><div><p className="text-sm font-semibold leading-5 text-foreground">{focusedDateTitle}</p><p className="text-[11px] text-muted-foreground">30 минутын нарийвчлалтай day timeline</p></div></div><div className="mt-2 grid text-[10px] text-muted-foreground" style={{ gridTemplateColumns: `repeat(${DAY_VIEW_SLOT_COUNT}, minmax(0, 1fr))` }}>{DAY_VIEW_SLOT_INDEXES.map((slot) => <span key={slot}>{slotToTime(slot, DAY_VIEW_SLOT_MINUTES)}</span>)}</div></div></div>
      {loading && rooms.length === 0 ? <div className="flex h-64 items-center justify-center text-sm text-muted-foreground">Ачаалж байна...</div> : errorMessage ? <div className="m-4 rounded-md border border-destructive/30 bg-destructive/5 p-6 text-sm text-destructive">{errorMessage}</div> : rooms.length === 0 ? <div className="flex h-64 items-center justify-center text-sm text-muted-foreground">Анги олдсонгүй</div> : rooms.map((room) => { const dayEvents = events.filter((event) => event.roomId === room.id && eventOccursInWeekOnDay(event, focusedDayOfWeek, focusedDateIso)).sort((left, right) => timeToMinutes(left.startTime) - timeToMinutes(right.startTime)); const activeSelection = selection?.roomId === room.id && selection.dayOfWeek === focusedDayOfWeek ? selection : null; const selectedStart = activeSelection ? Math.min(activeSelection.startSlot, activeSelection.endSlot) : null; const selectedEnd = activeSelection ? Math.max(activeSelection.startSlot, activeSelection.endSlot) + 1 : null; return <div key={room.id} className="grid min-h-[84px] grid-cols-[104px_minmax(860px,1fr)] border-b border-[#edebe9] dark:border-border"><Link href={`/dashboard/room/${room.id}`} className="flex flex-col justify-center border-r border-[#e1dfdd] bg-[#faf9f8] px-2.5 py-2 transition-colors hover:bg-[#f3f2f1] dark:border-border dark:bg-muted/30"><span className="text-[15px] font-semibold leading-5 text-foreground">{room.number}</span><span className="text-[11px] text-muted-foreground">{room.type === 'lab' ? 'Lab' : 'Event hall'}</span></Link><div className="relative"><div className="absolute inset-0 grid" style={{ gridTemplateColumns: `repeat(${DAY_VIEW_SLOT_COUNT}, minmax(0, 1fr))` }}>{DAY_VIEW_SLOT_INDEXES.map((slot) => <div key={slot} className="border-r border-[#f0f1f8] last:border-r-0 dark:border-border/60" />)}</div><div className="absolute inset-0 grid" style={{ gridTemplateColumns: `repeat(${DAY_VIEW_SLOT_COUNT}, minmax(0, 1fr))` }}>{DAY_VIEW_SLOT_INDEXES.map((slot) => <button key={slot} type="button" className="relative z-[1] h-full cursor-crosshair border-r border-transparent outline-none transition-colors hover:bg-[#6264a7]/5" onPointerDown={(event) => onPointerDown(room.id, focusedDayOfWeek, slot, DAY_VIEW_SLOT_MINUTES, event.button)} onPointerEnter={() => onPointerEnter(room.id, focusedDayOfWeek, slot)} onContextMenu={(event) => onContextMenu(event, room.id, focusedDayOfWeek, slot, DAY_VIEW_SLOT_MINUTES)} aria-label={`${room.number} ${focusedDateLabel} ${slotToTime(slot, DAY_VIEW_SLOT_MINUTES)}`} />)}</div>{selectedStart != null && selectedEnd != null && <div className="pointer-events-none absolute bottom-2 top-2 z-[2] rounded-lg border border-[#6264a7] bg-[#6264a7]/15 shadow-[inset_0_0_0_1px_rgba(98,100,167,0.25)]" style={{ left: `${(selectedStart / DAY_VIEW_SLOT_COUNT) * 100}%`, width: `${((selectedEnd - selectedStart) / DAY_VIEW_SLOT_COUNT) * 100}%` }} />}{dayEvents.map((event) => { const position = clampEventToPlanningDay(event, DAY_VIEW_SLOT_MINUTES); if (!position) return null; return <button key={`${event.id}-${focusedDateIso}-${room.id}`} type="button" className={cn('absolute bottom-2 top-2 z-[3] overflow-hidden rounded-lg border border-[#d1d1d1] border-l-4 px-2.5 py-1.5 text-left text-xs shadow-sm transition-transform hover:-translate-y-0.5 hover:shadow-md', getEventTone(event.type))} style={{ left: `${position.left}%`, width: `${position.width}%` }} onClick={() => onEditEvent(event, room.id, focusedDayOfWeek)}><span className="block truncate text-[12px] font-semibold">{event.title}</span><span className="block truncate opacity-80">{event.startTime}-{event.endTime} · {getEventLabel(event.type)}</span>{event.notes && <span className="mt-1 block truncate opacity-70">{event.notes}</span>}</button> })}</div></div> })}
    </div>
  )
}
